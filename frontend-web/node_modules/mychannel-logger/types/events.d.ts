import { IEventOption } from "./interfaces";
export declare class EventLogger {
    private static timestampFormat;
    private static logger;
    static init(server: any, eventOption: IEventOption): void;
    private static handleLogError(err, requestInfo);
    private static isObject(obj);
    private static logBody(requestBody, hiddenFields);
    private static calculateResponseTime(request);
}
