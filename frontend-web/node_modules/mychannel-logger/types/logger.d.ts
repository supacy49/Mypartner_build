import { ILogOption, IInfoLog, IAccessLog, IRootLog, IServiceLog } from "./interfaces";
export default class Logger {
    private timestampFormat;
    private loggerFormat;
    private logOption;
    constructor(logOption: ILogOption);
    accessLog: (message: IAccessLog) => void;
    infoLog: (message: IInfoLog) => void;
    rootLog: (message: IRootLog) => void;
    serviceLog: (message: IServiceLog) => void;
    emptyLogFormat(logType: string, serviceName: string): string;
    private createLogger;
    private getTimestamp;
}
