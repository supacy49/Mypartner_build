stages:
  - build
  - test

image: mychannel/mychannel-base

cache:
  paths:
   - node_modules/

install_package:
  stage: build
  script:
    - rm -rf node_modules
    - yarn install

static code quality:
  stage: test
  script: npm run lint